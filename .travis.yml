before_install:
  - export NOKOGIRI_USE_SYSTEM_LIBRARIES=true

script:
  - bundle exec jekyll build --config _config.yml,_config_travis.yml --drafts
  - bundle exec htmlproof ./_site

language: ruby
rvm:
  - 2.1

notifications:
  email: false
